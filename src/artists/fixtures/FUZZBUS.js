import image from '../images/FUZZBUS.jpg'

export const url = ''
export const photo = image

// Markdown fields
export const name = 'FUZZ BUS  *(CY)**(LIVE)*'
export const genres = '*SOUL* *FUNK JAZZ*'
export const text = `
Fiercely energetic and compellingly unpredictable, the Fuzz Bus quartet are all about solid grooves and impromptu improvisation. New original material will be debuted at the festival this year, combining their own unique sound, with the band’s signature re-arrangements of known and unknown soul, funk and pop rock songs.
`.trim()

export default {
  name,
  genres,
  url,
  photo,
  text,
}
